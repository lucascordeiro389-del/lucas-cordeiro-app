/* ============================================
   DR. LUCAS CORDEIRO - FISIOTERAPEUTA
   Sistema de Gestão - PWA
   ============================================ */

/* CSS Variables - Paleta de Cores */
:root {
  /* Cores Principais */
  --primary: #5C7C5A;
  --primary-dark: #4A6548;
  --primary-light: #8FA88D;
  --primary-ultra-light: #E8F0E7;
  
  /* Cores de Destaque */
  --accent: #A0674B;
  --accent-light: #C4917A;
  --accent-dark: #7D4F39;
  
  /* Neutros */
  --cream: #E8E0D5;
  --cream-dark: #D4C8B8;
  --cream-light: #F5F1EB;
  --white: #FDFCFA;
  
  /* Textos */
  --text-dark: #2D3A2C;
  --text-medium: #5A6659;
  --text-light: #8A9488;
  --text-muted: #A8B0A7;
  
  /* Status */
  --success: #6B8E6B;
  --warning: #D4A556;
  --danger: #C76B6B;
  --info: #6B8EA8;
  
  /* Tipos de Serviço */
  --pilates: #7B68A6;
  --reabilitacao: #5C7C9A;
  --terapia: #6B9A7C;
  
  /* Layout */
  --header-height: 70px;
  --nav-height: 70px;
  --radius: 12px;
  --radius-lg: 20px;
  --shadow: 0 2px 12px rgba(45, 58, 44, 0.08);
  --shadow-lg: 0 8px 32px rgba(45, 58, 44, 0.12);
  
  /* Transitions */
  --transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
}

/* Reset & Base */
*, *::before, *::after {
  box-sizing: border-box;
  margin: 0;
  padding: 0;
}

html {
  font-size: 16px;
  -webkit-tap-highlight-color: transparent;
}

body {
  font-family: 'Montserrat', -apple-system, BlinkMacSystemFont, sans-serif;
  background: var(--cream-light);
  color: var(--text-dark);
  line-height: 1.5;
  overflow-x: hidden;
  min-height: 100vh;
  min-height: 100dvh;
}

/* Typography */
h1, h2, h3, h4 {
  font-family: 'Cormorant Garamond', Georgia, serif;
  font-weight: 600;
  letter-spacing: -0.02em;
}

/* ============================================
   SPLASH SCREEN
   ============================================ */
.splash-screen {
  position: fixed;
  inset: 0;
  background: linear-gradient(145deg, var(--primary) 0%, var(--primary-dark) 100%);
  display: flex;
  align-items: center;
  justify-content: center;
  z-index: 9999;
  transition: opacity 0.5s ease, visibility 0.5s ease;
}

.splash-screen.fade-out {
  opacity: 0;
  visibility: hidden;
}

.splash-content {
  text-align: center;
  animation: fadeInUp 0.8s ease;
}

.splash-logo {
  width: 100px;
  height: 100px;
  margin: 0 auto 24px;
}

.logo-svg {
  width: 100%;
  height: 100%;
}

.spine-path {
  stroke-dasharray: 200;
  stroke-dashoffset: 200;
  animation: drawPath 1.5s ease forwards 0.3s;
}

.vertebra {
  opacity: 0;
  animation: fadeIn 0.3s ease forwards;
}

.vertebra.v1 { animation-delay: 0.5s; }
.vertebra.v2 { animation-delay: 0.6s; }
.vertebra.v3 { animation-delay: 0.7s; }
.vertebra.v4 { animation-delay: 0.8s; }
.vertebra.v5 { animation-delay: 0.9s; }
.vertebra.v6 { animation-delay: 1s; }

.splash-title {
  font-family: 'Cormorant Garamond', serif;
  font-size: 2.5rem;
  font-weight: 500;
  color: var(--cream);
  margin-bottom: 4px;
  letter-spacing: 0.02em;
}

.splash-subtitle {
  font-size: 0.75rem;
  font-weight: 500;
  color: var(--cream-dark);
  letter-spacing: 0.3em;
  margin-bottom: 32px;
}

.splash-loader {
  width: 120px;
  height: 3px;
  background: rgba(232, 224, 213, 0.2);
  border-radius: 3px;
  margin: 0 auto;
  overflow: hidden;
}

.loader-bar {
  height: 100%;
  width: 0;
  background: var(--cream);
  border-radius: 3px;
  animation: loadBar 1.8s ease forwards 0.5s;
}

/* ============================================
   HEADER
   ============================================ */
.header {
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  height: var(--header-height);
  background: linear-gradient(145deg, var(--primary) 0%, var(--primary-dark) 100%);
  z-index: 100;
  padding: 0 16px;
  display: flex;
  align-items: center;
}

.header-content {
  width: 100%;
  max-width: 600px;
  margin: 0 auto;
  display: flex;
  align-items: center;
  justify-content: space-between;
}

.header-brand {
  display: flex;
  align-items: center;
  gap: 12px;
}

.header-logo {
  width: 36px;
  height: 36px;
}

.header-text {
  display: flex;
  flex-direction: column;
}

.header-name {
  font-family: 'Cormorant Garamond', serif;
  font-size: 1.25rem;
  font-weight: 500;
  color: var(--cream);
  line-height: 1.2;
}

.header-date {
  font-size: 0.7rem;
  color: var(--cream-dark);
  font-weight: 400;
}

.header-btn {
  width: 40px;
  height: 40px;
  border-radius: 50%;
  border: none;
  background: rgba(255, 255, 255, 0.1);
  color: var(--cream);
  cursor: pointer;
  display: flex;
  align-items: center;
  justify-content: center;
  transition: var(--transition);
}

.header-btn:hover {
  background: rgba(255, 255, 255, 0.2);
}

/* ============================================
   MAIN CONTENT
   ============================================ */
.app.hidden {
  display: none;
}

.main-content {
  padding-top: var(--header-height);
  padding-bottom: var(--nav-height);
  min-height: 100vh;
  min-height: 100dvh;
}

.page {
  display: none;
  padding: 16px;
  max-width: 600px;
  margin: 0 auto;
  animation: fadeIn 0.3s ease;
}

.page.active {
  display: block;
}

.page-header {
  margin-bottom: 16px;
}

.page-title {
  font-size: 1.5rem;
  color: var(--text-dark);
  margin-bottom: 12px;
}

.page-stats {
  display: flex;
  gap: 8px;
  flex-wrap: wrap;
}

.stat-badge {
  font-size: 0.7rem;
  font-weight: 600;
  padding: 4px 10px;
  border-radius: 20px;
  color: white;
}

.stat-badge.pilates { background: var(--pilates); }
.stat-badge.reab { background: var(--reabilitacao); }
.stat-badge.terapia { background: var(--terapia); }

/* ============================================
   SEARCH & FILTERS
   ============================================ */
.search-container {
  position: relative;
  margin-bottom: 12px;
}

.search-icon {
  position: absolute;
  left: 14px;
  top: 50%;
  transform: translateY(-50%);
  color: var(--text-muted);
  font-size: 1.2rem;
}

.search-input {
  width: 100%;
  padding: 12px 16px 12px 44px;
  border: 2px solid var(--cream-dark);
  border-radius: var(--radius);
  font-size: 0.95rem;
  font-family: inherit;
  background: var(--white);
  color: var(--text-dark);
  transition: var(--transition);
}

.search-input:focus {
  outline: none;
  border-color: var(--primary);
  box-shadow: 0 0 0 3px var(--primary-ultra-light);
}

.filter-tabs {
  display: flex;
  gap: 8px;
  overflow-x: auto;
  padding-bottom: 4px;
  -webkit-overflow-scrolling: touch;
}

.filter-tabs::-webkit-scrollbar {
  display: none;
}

.filter-tab {
  padding: 8px 16px;
  border: 2px solid var(--cream-dark);
  border-radius: 20px;
  background: var(--white);
  color: var(--text-medium);
  font-size: 0.8rem;
  font-weight: 500;
  font-family: inherit;
  cursor: pointer;
  white-space: nowrap;
  transition: var(--transition);
}

.filter-tab:hover {
  border-color: var(--primary-light);
}

.filter-tab.active {
  background: var(--primary);
  border-color: var(--primary);
  color: white;
}

/* ============================================
   CARDS
   ============================================ */
.card {
  background: var(--white);
  border-radius: var(--radius);
  padding: 16px;
  margin-bottom: 12px;
  box-shadow: var(--shadow);
  transition: var(--transition);
  cursor: pointer;
  border-left: 4px solid transparent;
}

.card:hover {
  transform: translateY(-2px);
  box-shadow: var(--shadow-lg);
}

.card.pilates { border-left-color: var(--pilates); }
.card.reabilitacao { border-left-color: var(--reabilitacao); }
.card.terapia { border-left-color: var(--terapia); }

.card-header {
  display: flex;
  justify-content: space-between;
  align-items: flex-start;
  margin-bottom: 8px;
}

.card-title {
  font-family: 'Cormorant Garamond', serif;
  font-size: 1.1rem;
  font-weight: 600;
  color: var(--text-dark);
}

.card-badge {
  font-size: 0.65rem;
  font-weight: 600;
  padding: 3px 8px;
  border-radius: 12px;
  text-transform: uppercase;
  letter-spacing: 0.05em;
}

.card-badge.pilates { background: var(--pilates); color: white; }
.card-badge.reabilitacao { background: var(--reabilitacao); color: white; }
.card-badge.terapia { background: var(--terapia); color: white; }
.card-badge.domicilio { background: var(--accent); color: white; }

.card-info {
  display: flex;
  flex-wrap: wrap;
  gap: 12px;
  font-size: 0.85rem;
  color: var(--text-light);
}

.card-info-item {
  display: flex;
  align-items: center;
  gap: 4px;
}

.card-info-item .material-icons-round {
  font-size: 1rem;
}

/* ============================================
   WEEK NAVIGATION
   ============================================ */
.week-nav {
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 16px;
}

.week-btn {
  width: 36px;
  height: 36px;
  border-radius: 50%;
  border: 2px solid var(--cream-dark);
  background: var(--white);
  color: var(--text-medium);
  cursor: pointer;
  display: flex;
  align-items: center;
  justify-content: center;
  transition: var(--transition);
}

.week-btn:hover {
  border-color: var(--primary);
  color: var(--primary);
}

.week-title {
  font-family: 'Cormorant Garamond', serif;
  font-size: 1.2rem;
  font-weight: 600;
  color: var(--text-dark);
  min-width: 180px;
  text-align: center;
}

/* ============================================
   AGENDA GRID
   ============================================ */
.agenda-grid-container {
  overflow-x: auto;
  -webkit-overflow-scrolling: touch;
}

.agenda-grid {
  display: grid;
  grid-template-columns: 50px repeat(6, minmax(100px, 1fr));
  gap: 2px;
  min-width: 650px;
}

.agenda-header {
  background: var(--primary);
  color: var(--cream);
  padding: 10px 6px;
  text-align: center;
  font-size: 0.75rem;
  font-weight: 600;
  position: sticky;
  top: 0;
}

.agenda-header.hoje {
  background: var(--accent);
}

.agenda-header .dia-num {
  font-family: 'Cormorant Garamond', serif;
  font-size: 1.3rem;
  font-weight: 700;
}

.agenda-hora {
  background: var(--cream);
  padding: 8px 4px;
  font-size: 0.7rem;
  font-weight: 600;
  color: var(--text-medium);
  text-align: center;
  border-bottom: 1px solid var(--cream-dark);
}

.agenda-slot {
  background: var(--white);
  min-height: 50px;
  padding: 4px;
  border-bottom: 1px solid var(--cream);
  border-right: 1px solid var(--cream);
  transition: var(--transition);
}

.agenda-slot:hover {
  background: var(--cream-light);
}

.agenda-slot.vazio {
  cursor: pointer;
}

.agenda-item {
  font-size: 0.65rem;
  padding: 4px 6px;
  border-radius: 4px;
  margin-bottom: 2px;
  color: white;
  cursor: pointer;
  transition: var(--transition);
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.agenda-item:hover {
  transform: scale(1.02);
}

.agenda-item.pilates { background: var(--pilates); }
.agenda-item.reabilitacao { background: var(--reabilitacao); }
.agenda-item.terapia { background: var(--terapia); }
.agenda-item.realizado { opacity: 0.6; }

/* ============================================
   BOTTOM NAVIGATION
   ============================================ */
.bottom-nav {
  position: fixed;
  bottom: 0;
  left: 0;
  right: 0;
  height: var(--nav-height);
  background: var(--white);
  display: flex;
  align-items: center;
  justify-content: space-around;
  padding: 0 8px;
  box-shadow: 0 -4px 20px rgba(45, 58, 44, 0.08);
  z-index: 100;
}

.nav-item {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  gap: 2px;
  padding: 8px 12px;
  border: none;
  background: none;
  color: var(--text-muted);
  cursor: pointer;
  transition: var(--transition);
  font-family: inherit;
}

.nav-item .material-icons-round {
  font-size: 1.5rem;
}

.nav-label {
  font-size: 0.65rem;
  font-weight: 500;
}

.nav-item.active {
  color: var(--primary);
}

.nav-item.active .material-icons-round {
  color: var(--primary);
}

.fab-container {
  position: relative;
  padding: 0;
}

.fab {
  width: 56px;
  height: 56px;
  border-radius: 50%;
  background: linear-gradient(145deg, var(--primary) 0%, var(--primary-dark) 100%);
  color: white;
  display: flex;
  align-items: center;
  justify-content: center;
  box-shadow: 0 4px 16px rgba(92, 124, 90, 0.4);
  transform: translateY(-8px);
  transition: var(--transition);
}

.fab:hover {
  transform: translateY(-12px);
  box-shadow: 0 8px 24px rgba(92, 124, 90, 0.5);
}

.fab .material-icons-round {
  font-size: 1.8rem;
}

/* ============================================
   MODAL
   ============================================ */
.modal-overlay {
  position: fixed;
  inset: 0;
  background: rgba(45, 58, 44, 0.5);
  backdrop-filter: blur(4px);
  display: flex;
  align-items: flex-end;
  justify-content: center;
  z-index: 200;
  opacity: 0;
  visibility: hidden;
  transition: var(--transition);
}

.modal-overlay.active {
  opacity: 1;
  visibility: visible;
}

.modal {
  width: 100%;
  max-width: 500px;
  max-height: 90vh;
  background: var(--white);
  border-radius: var(--radius-lg) var(--radius-lg) 0 0;
  transform: translateY(100%);
  transition: transform 0.4s cubic-bezier(0.4, 0, 0.2, 1);
  display: flex;
  flex-direction: column;
}

.modal-overlay.active .modal {
  transform: translateY(0);
}

.modal-header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 20px 20px 16px;
  border-bottom: 1px solid var(--cream);
}

.modal-title {
  font-family: 'Cormorant Garamond', serif;
  font-size: 1.4rem;
  font-weight: 600;
  color: var(--text-dark);
}

.modal-close {
  width: 36px;
  height: 36px;
  border-radius: 50%;
  border: none;
  background: var(--cream);
  color: var(--text-medium);
  cursor: pointer;
  display: flex;
  align-items: center;
  justify-content: center;
  transition: var(--transition);
}

.modal-close:hover {
  background: var(--cream-dark);
}

.modal-body {
  padding: 20px;
  overflow-y: auto;
  flex: 1;
}

/* ============================================
   FORMS
   ============================================ */
.form-group {
  margin-bottom: 16px;
}

.form-label {
  display: block;
  font-size: 0.8rem;
  font-weight: 600;
  color: var(--text-medium);
  margin-bottom: 6px;
  text-transform: uppercase;
  letter-spacing: 0.05em;
}

.form-input,
.form-select,
.form-textarea {
  width: 100%;
  padding: 12px 14px;
  border: 2px solid var(--cream-dark);
  border-radius: var(--radius);
  font-size: 0.95rem;
  font-family: inherit;
  background: var(--white);
  color: var(--text-dark);
  transition: var(--transition);
}

.form-input:focus,
.form-select:focus,
.form-textarea:focus {
  outline: none;
  border-color: var(--primary);
  box-shadow: 0 0 0 3px var(--primary-ultra-light);
}

.form-textarea {
  resize: vertical;
  min-height: 80px;
}

.form-row {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 12px;
}

.form-hint {
  font-size: 0.75rem;
  color: var(--text-muted);
  margin-top: 4px;
}

/* Radio/Checkbox Group */
.radio-group,
.checkbox-group {
  display: flex;
  flex-wrap: wrap;
  gap: 8px;
}

.radio-option,
.checkbox-option {
  position: relative;
}

.radio-option input,
.checkbox-option input {
  position: absolute;
  opacity: 0;
  pointer-events: none;
}

.radio-label,
.checkbox-label {
  display: inline-flex;
  align-items: center;
  gap: 6px;
  padding: 8px 14px;
  border: 2px solid var(--cream-dark);
  border-radius: 20px;
  font-size: 0.85rem;
  cursor: pointer;
  transition: var(--transition);
}

.radio-option input:checked + .radio-label,
.checkbox-option input:checked + .checkbox-label {
  background: var(--primary);
  border-color: var(--primary);
  color: white;
}

/* Buttons */
.btn {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  gap: 8px;
  padding: 12px 20px;
  border: none;
  border-radius: var(--radius);
  font-size: 0.9rem;
  font-weight: 600;
  font-family: inherit;
  cursor: pointer;
  transition: var(--transition);
}

.btn-primary {
  background: linear-gradient(145deg, var(--primary) 0%, var(--primary-dark) 100%);
  color: white;
  box-shadow: 0 4px 12px rgba(92, 124, 90, 0.3);
}

.btn-primary:hover {
  transform: translateY(-2px);
  box-shadow: 0 6px 16px rgba(92, 124, 90, 0.4);
}

.btn-secondary {
  background: var(--cream);
  color: var(--text-dark);
}

.btn-secondary:hover {
  background: var(--cream-dark);
}

.btn-accent {
  background: linear-gradient(145deg, var(--accent) 0%, var(--accent-dark) 100%);
  color: white;
}

.btn-outline {
  background: transparent;
  border: 2px solid var(--primary);
  color: var(--primary);
}

.btn-outline:hover {
  background: var(--primary);
  color: white;
}

.btn-block {
  width: 100%;
}

.btn-sm {
  padding: 8px 12px;
  font-size: 0.8rem;
}

.form-actions {
  display: flex;
  gap: 12px;
  margin-top: 20px;
}

.form-actions .btn {
  flex: 1;
}

/* ============================================
   MENU LIST
   ============================================ */
.menu-list {
  display: flex;
  flex-direction: column;
  gap: 8px;
}

.menu-item {
  display: flex;
  align-items: center;
  gap: 14px;
  padding: 16px;
  background: var(--white);
  border: none;
  border-radius: var(--radius);
  font-size: 0.95rem;
  font-weight: 500;
  font-family: inherit;
  color: var(--text-dark);
  cursor: pointer;
  transition: var(--transition);
  box-shadow: var(--shadow);
  text-align: left;
}

.menu-item:hover {
  transform: translateX(4px);
  background: var(--cream-light);
}

.menu-item .material-icons-round {
  color: var(--primary);
}

.menu-item .arrow {
  margin-left: auto;
  color: var(--text-muted);
}

/* ============================================
   FINANCIAL CARDS
   ============================================ */
.finance-summary {
  display: grid;
  grid-template-columns: repeat(2, 1fr);
  gap: 12px;
  margin-bottom: 20px;
}

.finance-card {
  background: var(--white);
  border-radius: var(--radius);
  padding: 16px;
  text-align: center;
  box-shadow: var(--shadow);
}

.finance-card.highlight {
  background: linear-gradient(145deg, var(--primary) 0%, var(--primary-dark) 100%);
  color: white;
  grid-column: span 2;
}

.finance-value {
  font-family: 'Cormorant Garamond', serif;
  font-size: 1.6rem;
  font-weight: 700;
  margin-bottom: 4px;
}

.finance-card.highlight .finance-value {
  font-size: 2rem;
}

.finance-label {
  font-size: 0.7rem;
  font-weight: 600;
  text-transform: uppercase;
  letter-spacing: 0.05em;
  opacity: 0.8;
}

/* ============================================
   TOAST
   ============================================ */
.toast-container {
  position: fixed;
  top: calc(var(--header-height) + 16px);
  left: 50%;
  transform: translateX(-50%);
  z-index: 300;
  display: flex;
  flex-direction: column;
  gap: 8px;
  pointer-events: none;
}

.toast {
  background: var(--text-dark);
  color: white;
  padding: 12px 20px;
  border-radius: var(--radius);
  font-size: 0.9rem;
  font-weight: 500;
  box-shadow: var(--shadow-lg);
  animation: toastIn 0.3s ease, toastOut 0.3s ease 2.7s forwards;
  pointer-events: auto;
}

.toast.success { background: var(--success); }
.toast.warning { background: var(--warning); }
.toast.error { background: var(--danger); }
.toast.info { background: var(--info); }

/* ============================================
   EMPTY STATE
   ============================================ */
.empty-state {
  text-align: center;
  padding: 48px 24px;
  color: var(--text-light);
}

.empty-state .material-icons-round {
  font-size: 4rem;
  color: var(--cream-dark);
  margin-bottom: 16px;
}

.empty-state h3 {
  font-family: 'Cormorant Garamond', serif;
  font-size: 1.3rem;
  color: var(--text-medium);
  margin-bottom: 8px;
}

.empty-state p {
  font-size: 0.9rem;
  margin-bottom: 20px;
}

/* ============================================
   LOADING
   ============================================ */
.loading {
  display: flex;
  align-items: center;
  justify-content: center;
  padding: 48px;
}

.spinner {
  width: 40px;
  height: 40px;
  border: 3px solid var(--cream);
  border-top-color: var(--primary);
  border-radius: 50%;
  animation: spin 0.8s linear infinite;
}

/* ============================================
   HORARIOS GRID (Agenda Hoje)
   ============================================ */
.horarios-hoje {
  display: flex;
  flex-direction: column;
  gap: 8px;
}

.horario-row {
  display: flex;
  gap: 12px;
  align-items: flex-start;
}

.horario-time {
  width: 50px;
  font-size: 0.8rem;
  font-weight: 600;
  color: var(--text-medium);
  padding-top: 12px;
  text-align: right;
}

.horario-content {
  flex: 1;
  min-height: 60px;
  background: var(--cream-light);
  border-radius: var(--radius);
  padding: 8px;
  border: 2px dashed var(--cream-dark);
  transition: var(--transition);
}

.horario-content:hover {
  border-color: var(--primary-light);
  background: var(--white);
}

.horario-content.ocupado {
  border: none;
  background: var(--white);
  padding: 0;
}

.atendimento-card {
  padding: 12px;
  border-radius: var(--radius);
  margin-bottom: 6px;
  color: white;
  cursor: pointer;
  transition: var(--transition);
}

.atendimento-card:last-child {
  margin-bottom: 0;
}

.atendimento-card:hover {
  transform: scale(1.02);
}

.atendimento-card.pilates { background: var(--pilates); }
.atendimento-card.reabilitacao { background: var(--reabilitacao); }
.atendimento-card.terapia { background: var(--terapia); }

.atendimento-card.realizado {
  opacity: 0.7;
}

.atendimento-card.faltou {
  opacity: 0.5;
  text-decoration: line-through;
}

.atendimento-nome {
  font-weight: 600;
  font-size: 0.95rem;
  margin-bottom: 2px;
}

.atendimento-info {
  font-size: 0.75rem;
  opacity: 0.9;
  display: flex;
  align-items: center;
  gap: 8px;
}

.atendimento-actions {
  display: flex;
  gap: 6px;
  margin-top: 8px;
}

.atendimento-btn {
  padding: 4px 10px;
  border-radius: 12px;
  border: none;
  font-size: 0.7rem;
  font-weight: 600;
  font-family: inherit;
  cursor: pointer;
  transition: var(--transition);
  background: rgba(255, 255, 255, 0.2);
  color: white;
}

.atendimento-btn:hover {
  background: rgba(255, 255, 255, 0.3);
}

/* ============================================
   STATUS BADGES
   ============================================ */
.status-badge {
  display: inline-flex;
  align-items: center;
  gap: 4px;
  font-size: 0.7rem;
  font-weight: 600;
  padding: 3px 8px;
  border-radius: 12px;
}

.status-pago {
  background: rgba(107, 142, 107, 0.15);
  color: var(--success);
}

.status-pendente {
  background: rgba(212, 165, 86, 0.15);
  color: var(--warning);
}

.status-atrasado {
  background: rgba(199, 107, 107, 0.15);
  color: var(--danger);
}

.status-pausado {
  background: rgba(160, 103, 75, 0.15);
  color: var(--accent);
}

/* ============================================
   ANIMATIONS
   ============================================ */
@keyframes fadeIn {
  from { opacity: 0; }
  to { opacity: 1; }
}

@keyframes fadeInUp {
  from {
    opacity: 0;
    transform: translateY(20px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

@keyframes drawPath {
  to { stroke-dashoffset: 0; }
}

@keyframes loadBar {
  to { width: 100%; }
}

@keyframes spin {
  to { transform: rotate(360deg); }
}

@keyframes toastIn {
  from {
    opacity: 0;
    transform: translateY(-20px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

@keyframes toastOut {
  to {
    opacity: 0;
    transform: translateY(-20px);
  }
}

/* ============================================
   RESPONSIVE
   ============================================ */
@media (min-width: 768px) {
  .modal {
    max-height: 85vh;
    border-radius: var(--radius-lg);
    margin: auto;
  }
  
  .modal-overlay {
    align-items: center;
    padding: 20px;
  }
}

/* PWA Safe Areas */
@supports (padding: max(0px)) {
  .header {
    padding-top: max(0px, env(safe-area-inset-top));
    height: calc(var(--header-height) + max(0px, env(safe-area-inset-top)));
  }
  
  .bottom-nav {
    padding-bottom: max(8px, env(safe-area-inset-bottom));
    height: calc(var(--nav-height) + max(0px, env(safe-area-inset-bottom)));
  }
  
  .main-content {
    padding-top: calc(var(--header-height) + max(0px, env(safe-area-inset-top)));
    padding-bottom: calc(var(--nav-height) + max(0px, env(safe-area-inset-bottom)));
  }
}
